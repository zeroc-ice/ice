To run this demo, open two terminal windows. In the first window,
start the IceBox server:

$ ../../../bin/iceboxnet.exe --Ice.Config=config.icebox

In the second window, run the client:

$ client.exe
