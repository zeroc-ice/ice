name: "Build Maven Packages"

on:
  workflow_call:
    inputs:
      ice_version:
        required: false
        type: string
  workflow_dispatch:
    inputs:
      ice_version:
        description: "The Ice version to build"
        required: false
jobs:
  build-maven-packages:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Setup C++
        uses: ./.github/actions/setup-cpp

      - name: Setup Java
        uses: ./.github/actions/setup-java

      - name: Update Ice Version
        run: |
          sed -i "s/^iceVersion = .*/iceVersion = ${{ inputs.ice_version }}/" java/gradle.properties
          sed -i "s/^iceVersion = .*/iceVersion = ${{ inputs.ice_version }}/" java-compat/gradle.properties
        if: ${{ inputs.ice_version != '' }}

      - name: Build Slice Compilers
        run: make -C cpp slice2java

      - name: Build Ice for Java Packages
        run: ./gradlew dist
        working-directory: java

      - name: Build Ice for Java Compat Packages
        run: ./gradlew dist
        working-directory: java-compat

      - name: Upload Artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: java-packages
          path: |
            java/lib/*.jar
            java/lib/*.pom
            java-compat/lib/*.jar
            java-compat/lib/*.pom
