version = 3.6.5
mono_root = /usr

name = Glacier2

Name: ${name}
Description: Glacier2 run-time support
Version: ${version}
Libs: -r:${mono_root}/lib/mono/${name}/${name}.dll
Requires: Ice = ${version}
