diff -ruN openssl-1.0.1c/Makefile.shared openssl-1.0.1c_patched/Makefile.shared
--- openssl-1.0.1c/Makefile.shared	2010-08-21 13:37:17.000000000 +0200
+++ openssl-1.0.1c_patched/Makefile.shared	2012-11-15 00:37:44.077563309 +0100
@@ -283,10 +283,10 @@
 	base=-Wl,--enable-auto-image-base; \
 	if expr $(PLATFORM) : 'mingw' > /dev/null; then \
 		case $(LIBNAME) in \
-			crypto) SHLIB=libeay;; \
-			ssl) SHLIB=ssleay;; \
+			crypto) SHLIB=libeay32_mingw;; \
+			ssl) SHLIB=ssleay32_mingw;; \
 		esac; \
-		SHLIB_SOVER=32; \
+		SHLIB_SOVER=; \
 		extras="$(LIBNAME).def"; \
 		$(PERL) util/mkdef.pl 32 $$SHLIB > $$extras; \
 		base=; [ $(LIBNAME) = "crypto" ] && base=-Wl,--image-base,0x63000000; \
diff -ruN openssl-1.0.1c/util/mkdef.pl openssl-1.0.1c_patched/util/mkdef.pl
--- openssl-1.0.1c/util/mkdef.pl	2012-01-01 00:49:45.000000000 +0100
+++ openssl-1.0.1c_patched/util/mkdef.pl	2012-11-15 00:37:58.997563951 +0100
@@ -168,12 +168,12 @@
 		$zlib = 1;
 	}
 
-	$do_ssl=1 if $_ eq "ssleay";
+	$do_ssl=1 if $_ eq "ssleay32_mingw";
 	if ($_ eq "ssl") {
 		$do_ssl=1; 
 		$libname=$_
 	}
-	$do_crypto=1 if $_ eq "libeay";
+	$do_crypto=1 if $_ eq "libeay32_mingw";
 	if ($_ eq "crypto") {
 		$do_crypto=1;
 		$libname=$_;
@@ -240,10 +240,10 @@
 
 if (!$libname) { 
 	if ($do_ssl) {
-		$libname="SSLEAY";
+		$libname="SSLEAY32_MINGW";
 	}
 	if ($do_crypto) {
-		$libname="LIBEAY";
+		$libname="LIBEAY32_MINGW";
 	}
 }
 
@@ -1283,7 +1283,7 @@
 	my $description = "$what $version, $name - http://$http_vendor";
 
 	if ($W32)
-		{ $libname.="32"; }
+		{ $libname.=""; }
 	elsif ($W16)
 		{ $libname.="16"; }
 	elsif ($OS2)
