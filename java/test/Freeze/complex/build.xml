<!--
 **********************************************************************

 Copyright (c) 2003-2006 ZeroC, Inc. All rights reserved.

 This copy of Ice is licensed to you under the terms described in the
 ICE_LICENSE file included in this distribution.

 **********************************************************************
-->

<project name="test_Freeze_cursor" default="all" basedir=".">

    <!-- set global properties for this build -->
    <property name="top.dir" value="../../.."/>
    <property name="lib.dir" value="${top.dir}/lib"/>
    <property name="class.dir" value="classes"/>
    <property name="generated.dir" value="generated"/>

    <!-- set common build properties for this build -->
    <property file="${top.dir}/config/build.properties"/>

    <!-- install slice2java task -->
    <taskdef name="slice2java" classpath="${top.dir}/ant"
        classname="Slice2JavaTask" />

    <!-- install slice2freezej task -->
    <taskdef name="slice2freezej" classpath="${top.dir}/ant"
        classname="Slice2FreezeJTask" />

    <target name="init">
        <!-- Create the time stamp -->
        <tstamp/>
    </target>

    <target name="generate" depends="init">
        <!-- Create the output directory for generated code -->
        <mkdir dir="${generated.dir}"/>
        <slice2java outputdir="${generated.dir}">
            <fileset dir="." includes="Complex.ice"/>
        </slice2java>
        <slice2freezej outputdir="${generated.dir}">
            <fileset dir="." includes="Complex.ice"/>
            <dict name="Complex.ComplexDict" key="Complex::Key" value="Complex::Node"/>
        </slice2freezej>
    </target>

    <target name="compile" depends="generate">
        <mkdir dir="${class.dir}"/>
        <javac srcdir="${generated.dir}" destdir="${class.dir}"
            source="1.4" classpath="${lib.dir}" debug="${debug}"/>
        <javac srcdir="." destdir="${class.dir}" source="1.4"
            classpath="${lib.dir}" excludes="generated/**" debug="${debug}"/>
    </target>

    <target name="all" depends="compile"/>

    <target name="clean">
        <delete dir="${class.dir}"/>
        <delete dir="${generated.dir}"/>
        <delete>
	    <fileset dir="db" includes="log.*"/>
            <fileset dir="db" includes="test"/>
        </delete>
    </target>

</project>
