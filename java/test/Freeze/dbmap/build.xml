<!--
 **********************************************************************

 Copyright (c) 2003-2005 ZeroC, Inc. All rights reserved.

 This copy of Ice is licensed to you under the terms described in the
 ICE_LICENSE file included in this distribution.

 **********************************************************************
-->

<!DOCTYPE project [
<!ENTITY common SYSTEM "file:../../../config/common.xml">
]>

<project name="test_Freeze_dbmap" default="all" basedir=".">

    <!-- set global properties for this build -->
    <property name="top.dir" value="../../.."/>

    <!-- Include common definitions -->
    &common;

    <property name="class.dir" value="classes"/>
    <property name="generated.dir" value="generated"/>

    <target name="init" depends="config-init">
        <!-- Create the time stamp -->
        <tstamp/>
    </target>

    <target name="generate" depends="init">
        <!-- Create the output directory for generated code -->
        <mkdir dir="${generated.dir}"/>
        <slice2freezej outputdir="${generated.dir}">
            <dict name="ByteIntMap" key="byte" value="int"/>
            <dictindex name="ByteIntMap"/>
        </slice2freezej>
        <slice2freezej ice="on" outputdir="${generated.dir}">
            <includepath>
                <pathelement path="${slice.dir}" />
            </includepath>
            <fileset dir="${slice.dir}/Ice" includes="Identity.ice"/>
            <dict name="IntIdentityMap" key="int" value="Ice::Identity"/>
        </slice2freezej>
        <slice2freezej ice="on" outputdir="${generated.dir}">
           <includepath>
                <pathelement path="${slice.dir}" />
            </includepath>
            <fileset dir="${slice.dir}/Ice" includes="Identity.ice"/>
           <dict name="IntIdentityMapWithIndex" key="int" value="Ice::Identity"/>
           <dictindex name="IntIdentityMapWithIndex" member="category"/>  
        </slice2freezej>
    </target>

    <target name="compile" depends="generate">
        <mkdir dir="${class.dir}"/>
        <javac srcdir="${generated.dir}" destdir="${class.dir}"
            source="1.4" classpath="${lib.dir}" debug="${debug}"/>
        <javac srcdir="." destdir="${class.dir}" source="1.4"
            classpath="${lib.dir}" excludes="generated/**" debug="${debug}"/>
    </target>

    <target name="all" depends="compile"/>

    <target name="clean">
        <delete dir="${class.dir}"/>
        <delete dir="${generated.dir}"/>
         <delete>
	    <fileset dir="db" includes="log.*"/>
            <fileset dir="db" includes="__catalog"/>
            <fileset dir="db" includes="binary,binary.index"/>
            <fileset dir="db" includes="intIdentity,intIdentity.category"/>
        </delete>
    </target>

</project>
