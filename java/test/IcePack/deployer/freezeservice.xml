<service kind="freeze" entry="FreezeService">

  <adapters>
    <adapter name="${name}" endpoints="default"  id="${parent}${name}Adapter">
      <object identity="${parent}-${name}" type="::Test"/>
    </adapter>
  </adapters>

  <properties>
    <property name="${name}.Identity" value="${parent}-${name}"/>
    <property name="${name}.Type" value="freeze"/>
    <property name="${name}.ServiceName" value="${name}"/>
  </properties>

  <target name="manual">
    <property name="Mode" value="manual"/>
  </target>

</service>