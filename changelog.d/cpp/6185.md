- Rejected negative `IceDiscovery.Timeout`, `IceLocatorDiscovery.Timeout`, and `IceLocatorDiscovery.RetryDelay`
  property values with `PropertyException` at plugin initialization, instead of silently scheduling a timer that
  fires immediately.
