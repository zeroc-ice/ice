- Fixed a crash in `slice2cpp` when compiling a doc comment with a tag that has no description, such as a bare
  `@param myParam` or `@throws MyException`. Such tags are valid: the description is optional.
