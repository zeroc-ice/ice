- Rejected a negative `IceDiscovery.Timeout` property value with `PropertyException` at plugin initialization,
  instead of letting it reach the timer.
- `IceLocatorDiscovery.Timeout` and `IceLocatorDiscovery.RetryDelay` now reject negative values with
  `PropertyException` at plugin initialization instead of silently clamping them to 300 ms and 0 ms respectively.
