- Fixed `ice_getConnection` and `ice_getConnectionAsync` on a proxy whose cached connection is closed: the proxy now
  establishes and returns a new connection instead of returning the closed connection. For a fixed proxy, these
  methods now throw the connection-closure exception, since a fixed proxy cannot establish a new connection.
