- Fixed the IcePy stub declaring keyword-capable parameters for functions that accept positional arguments only.
  Type checkers accepted calls such as `connection.setAdapter(adapter=None)` that raise `TypeError` at runtime;
  the stub and the docstrings now mark these parameters positional-only.
